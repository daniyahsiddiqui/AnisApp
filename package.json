{
  "name": "anisapp-athanclock",
  "version": "1.0.0",
  "description": "Smart Athan Clock App",
  "main": "app.js",
  "scripts": {
    "build": "export PATH=/Users/daniyah/node-lts/bin:$PATH && mkdir -p www && cp -R images www/ 2>/dev/null; cp index.html remote.html app.js remote.js style.css 1.mp3 www/ 2>/dev/null || cp -R images www/; cp index.html remote.html app.js remote.js style.css www/",
    "sync": "export PATH=/Users/daniyah/node-lts/bin:$PATH && npm run build && npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
